## FoodMoment — Ваш компаньон в еде.


1) Gateway с аутентификацией (AuthN/Z Gateway). NGinX 

На перспективу:
Клиент → API Gateway (проверяет JWT локально) → Бизнес-API (без auth-логики)
                ↓ (если токена нет)                     ↑
                auth-service (login, refresh)           │


Текущая реализация:
Клиент → Ваш API (публичный, порт 8080) → Auth-сервис (внутренний, порт 8081)


2) AuthService - сервис аутентификации клиента. 
TODO:
Какую аутентификацию выбрать?


3) FoodService - главный бизнес сервис. Точка входа для клиента.
Шаги:
API имеет хендлер /login, внутри делает HTTP-вызов к auth-сервису.
API проверяет JWT локально (публичный ключ получает при старте от auth-сервиса или из файла).
API — фактически и есть Gateway, только встроенный в код.

Выделяем auth-логику в middleware (подготовка к Gateway)
Оставьте в API поддержку двух режимов через переменную окружения

Чтобы Gateway был легко добавить позже, ваш API должен:

✅ Хорошо:
Принимать user_id в заголовке (например, X-User-Id).
Иметь конфигурацию "trusted gateway subnet" (192.168.0.0/16) — заголовки приходят только от Gateway, а не от клиента.
Быть stateless (не хранить сессии, токены, ничего локального).

❌ Не доверяйте X-User-Id от клиента. Только если запрос пришел с доверенного IP (ваш Gateway). Иначе любой сможет подставить чужой ID.


### Docker
Статус:              $systemctl status docker
Создание контейнера: $sudo docker run <имя-образа>
Остановка:           $sudo docker stop <ID-контейнера>
Удаление:            $sudo docker rm <ID-контейнера> 

Сборка образа:       $docker build -t yamdb .

        build — команда сборки образа по инструкциям из Dockerfile.
        -t yamdb — ключ, который позволяет задать имя образу, а потом и само имя.
        . — точка в конце команды — путь до Dockerfile, на основе которого производится сборка..

Запуск контейнера:   $docker run --name <имя контейнера> -it -p 8000:8000 yamdb

        run — команда запуска нового контейнера.
        --name my_project — ключ, который позволяет задать имя контейнеру, и само имя.
        -it — комбинация этих ключей даёт возможность передавать в контейнер команды из вашего терминала.
        -p 8000:8000 — указывает публичный порт контейнера. Левая часть — внешний порт контейнера, правая — порт, на который будет перенаправлен запрос.
        yamdb — образ, из которого будет запущен контейнер.

Какие контейнеры запущены: $docker container ls 
Зайти в контейнер:         $docker exec -it <CONTAINER ID> bash 

ENV:
Прописать переменные окружения прямо в докерфайле, для этого есть инструкция ENV: ENV token 12345.
Задать переменные окружения при сборке контейнера, выполнив команду run с ключом -e: docker run <IMAGE-NAME> -e token=12345.
Создать файл .env и прописать переменные окружения в нём. Это самый предпочтительный способ.

### Docker-compose
Запуск docker-compose к    $docker-compose up
# Запуск в фоновом режиме
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs kafka

# Остановка
docker-compose down